'use strict';

/**
 * pagina-servicios controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::pagina-servicios.pagina-servicios');
